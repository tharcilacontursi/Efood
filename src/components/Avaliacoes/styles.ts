import styled from "styled-components";
import { cores } from "../../styles";

export const NotaContainer = styled.div`
color: ${cores.laranja};
font-weight: bold;
display: flex;  
align-items: center;

img{
    width:20px ;
    height: 20px;
    margin-left: 4px;
}
`

